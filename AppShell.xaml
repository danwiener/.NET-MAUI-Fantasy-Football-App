<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="FantasyFootballMAUI.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:FantasyFootballMAUI"
    Shell.FlyoutBehavior="Flyout"
    FlyoutBackgroundColor="#438696"
    BackgroundColor="#233b66">

    <ShellContent
        Title="Welcome : Fantasy Football"
        ContentTemplate="{DataTemplate local:OpeningPage}"
        Route="OpeningPage"/>

    <ShellContent
        Title="Sign In"
        ContentTemplate="{DataTemplate local:LoginPage}"
        Route="LoginPage"/>

    <ShellContent
        Title="Sign Up"
        ContentTemplate="{DataTemplate local:RegisterPage}"
        Route="RegisterPage"/>

    <ShellContent
        Title="Sign Up"
        ContentTemplate="{DataTemplate local:LeaguesBelongedTo}"
        Route="LeaguesBelongedTo"/>



    <!--<Shell.ItemTemplate>
        <DataTemplate>
            <Grid ColumnDefinitions="50,*" Padding="10" >
                <Image Source="{Binding Icon}" />
                <Label Grid.Column="1" Text="{Binding Title}" TextColor="Black" FontAttributes="Bold" />
            </Grid>
        </DataTemplate>
    </Shell.ItemTemplate>
    <FlyoutItem Title="Home">
        <ShellContent
        Title="Home"
        ContentTemplate="{DataTemplate local:HomePage}"
        Route="HomePage" />
    </FlyoutItem>-->

</Shell>
